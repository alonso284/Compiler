program main{
    var i: int;
    var f: float;
    var ch: char;
    var s: string;
    var b: bool;

    begin;
        b := false;
        s := ch;
        b := (i < f and i <= f and i >= f and i > f) or (ch == s and s != ch) or (true and false) or (b == b);
    end;
}
